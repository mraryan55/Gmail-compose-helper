# Gmail Compose Helper

A simple Android APK that takes a subject line and directly opens Gmail's compose screen with the subject pre-filled.

## Features
- Enter subject text
- Redirects to Gmail compose screen
- Lightweight and easy to use

## How to Run
1. Clone the repository:
   ```bash
   git clone https://github.com/username/gmail-compose-helper.git
